import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  
  server: {
    watch: {
      // 这里的配置会被 Storybook 的 viteFinal 合并
      ignored: [
        '**/.nx/cache/**',
        '**/tsconfig*.json'
      ]
    }
  },
  
  // 其他配置...
});
